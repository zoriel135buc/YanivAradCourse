const utils = require('./arrUtils');

//let totalLen =  utils.getArrTotalLen(["Hello","OK","Bye"])

utils.getArrTotalLen(["Hello","OK","Bye"]).then(total => console.log(total));

console.log(totalLen);